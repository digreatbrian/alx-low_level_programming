#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Printing 0 to 9
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int nm;

	for (nm = 0 ; nm < 10 ; nm++)
	{
		printf("%d", nm);
	}
	return (0);
}
