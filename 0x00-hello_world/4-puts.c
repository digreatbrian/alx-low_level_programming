#include <stdio.h>

/**
 * main - Entry point
 *
 * Description : Prints the string "Programming is like building a
 * multilingual puzzle
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Assigning a variable for the output */
	char a[64] = "\"Programming is like building a multilingual puzzle";

	puts(a);
	return (0);
}
